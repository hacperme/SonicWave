# 服务器配置文件
# 端口号（默认 8089），也可以通过环境变量 PORT 覆盖
port = 8089

# 静态文件目录（相对路径或绝对路径）
static_dir = "."

# 缓存策略配置
# 开发环境推荐: "no-cache, no-store, must-revalidate"
# 生产环境推荐: "public, max-age=31536000, immutable"
# max-age 单位为秒，31536000 = 1年
cache_control = "public, max-age=31536000, immutable"
